---
"gh-aw": patch
---

Bump the default firewall version to v0.25.44, sync the embedded AWF config schema, and add compiler support for `firewall.effective-token-steering` and `firewall.model-multipliers` in generated AWF config.
