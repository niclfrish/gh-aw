"gh-aw": patch
---

Update default CLI/tool versions: Claude Code `2.1.116`, Copilot CLI `1.0.34`, and Codex `0.122.0`; then recompile workflow lock files so generated workflows consume the new versions.
