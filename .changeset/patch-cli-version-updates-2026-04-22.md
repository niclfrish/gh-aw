---
"gh-aw": patch
---

Update default versions for Claude Code (`2.1.117`), OpenAI Codex (`0.122.0`), and GitHub MCP Server (`v1.0.1`), and recompile workflow lock files. Keep Copilot CLI pinned at `1.0.21`.
